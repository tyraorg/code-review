{
  "name": "@tyraorg/code-review",
  "version": "0.0.1",
  "description": "Automated PR code review action using OpenAI",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --format=cjs --outfile=dist/index.cjs"
  },
  "dependencies": {
    "@actions/core": "3.0.1",
    "@actions/exec": "3.0.0",
    "@actions/github": "9.1.1",
    "@actions/http-client": "4.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
